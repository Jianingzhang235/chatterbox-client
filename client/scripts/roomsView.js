var RoomsView = {

  $button: $('#rooms button'),
  $select: $('#rooms select'),

  initialize: function(addRoom) {
    $(button).on(Click, function(e){
      //When the user clicks on the button we need to get the data from the form input field and store it
    })
    
  },

  render: function(newRoom) {
    // We take that name and create a div with the string for example 'susie's room
    // var ourDiv = <div></div>
    //this.$select.append(ourDiv)
  }

};
